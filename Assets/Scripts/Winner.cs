using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Winner : MonoBehaviour
{
    [SerializeField] TMP_Text showPlayerName;
    public int scoreHome;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    void OnWinner()
    {
       
        if (scoreHome > 0)
        {

        }
    }
}
